# Classification

There are two major supervised tasks: regression (predicting values) and classification (predicting classes). This chapter, is on Classification.
I already have a classification neural network that I did on the MNIST dataset a couple of months ago, but this will be a revision for that.

